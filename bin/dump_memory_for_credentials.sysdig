#!/bin/bash

echo -e `date +"%Y-%m-%d %H:%M:%S"` "\e[92m[T1003.007] Detect attempt to read mem or maps file in process folder to get credentials in plaintext\033[m"
cat /proc/$$/maps > /tmp/memory_dump
# Cleanup: remove the dumped memory map
rm -f /tmp/memory_dump
